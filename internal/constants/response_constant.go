package constants

const (
	PasswordMismatchError   string = "Passwords do not match"
	PasswordLengthError     string = "Password must be at least 8 characters long"
	PasswordComplexityError string = "Password must contain at least one uppercase letter, one lowercase letter, one digit, and one special character"

	PassedValidation string = "Passed validation"
)
